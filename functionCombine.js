/*Create a `function` that takes in two arguments and returns a concatenated string in the `console`.*/

function concatStr(str1, str2) {
  return str1 + str2;
}

console.log(concatStr('hello', ' there'))
